const styles = (theme) => ({
	title: {
		fontSize: '3rem',
		lineHeight: '3rem',
		textAlign: 'center',
		marginTop: theme.spacing(15),
		marginBottom: theme.spacing(3),
	},

	grid: {
		height: '100%',
	},
	carousel: { marginBottom: theme.spacing(5) },
});

export default styles;
